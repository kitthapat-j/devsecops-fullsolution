name: DevSecOps CI/CD Pipeline to Azure

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build_and_scan:
    name: 🛡️ Scan & Quality Gate
    permissions:
      contents: read
      packages: write
      pull-requests: write
      actions: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install Dependencies
        run: npm install --prefix app
        
      # 1. รัน SonarQube Scan
      - name: SonarQube Scan
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          npm run sonar --prefix app -- \
            -Dsonar.projectKey=kitthapat-j_devsecops-fullsolution \
            -Dsonar.organization=kitthapat-j \
            -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} \
            -Dsonar.token=${{ secrets.SONAR_TOKEN }}

      # 2. Security Gate: ตรวจสอบ Quality Gate (ใช้ Action สำรอง)
      - name: Wait for SonarQube Analysis and Check Quality Gate
        id: wait_sonar
        # ใช้ Action ที่ติดต่อกับ Sonar API โดยตรงและมีความเสถียรสูงกว่า
        uses: jbarratt/wait-on-sonar-action@v1.4.1
        with:
          url: ${{ secrets.SONAR_HOST_URL }}
          token: ${{ secrets.SONAR_TOKEN }}
          # ใส่ Project Key ที่คุณใช้งานอยู่
          projectKey: kitthapat-j_devsecops-fullsolution 
          timeout: 300 # รอผลลัพธ์สูงสุด 5 นาที
          
      - name: Fail if SonarQube Quality Gate Fails
        # ถ้าผลลัพธ์ที่ได้จาก Action ข้างบนไม่เป็น 'OK' (ผ่าน) ให้ Job นี้ Fail
        if: steps.wait_sonar.outputs.qualityGateStatus != 'OK'
        run: |
          echo "::error::SonarQube Quality Gate FAILED. Cannot proceed to testing/deployment."
          exit 1